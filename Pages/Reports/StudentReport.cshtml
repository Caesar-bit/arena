@page "{id:int}"
@model StudentGradeTrackingSystem.Pages.Reports.StudentReportModel
@{
    ViewData["Title"] = "Student Report";
}

<h1>Student Report for @Model.Student?.FullName</h1>
<p>ID: @Model.Student?.StudentNumber - Grade: @Model.Student?.GradeLevel - @Model.Student?.ContactEmail</p>

@if (Model.Student == null)
{
    <p>Student not found.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Course</th>
                <th>Assessment</th>
                <th>Score</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var grade in Model.Grades)
            {
                <tr>
                    <td>@grade.Course?.CourseName</td>
                    <td>@grade.AssessmentType (@grade.Weight%) - @grade.Score (Weighted: @grade.WeightedScore.ToString("F2"))</td>
                </tr>
            }
        </tbody>
    </table>
}

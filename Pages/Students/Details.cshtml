@page
@model StudentGradeTrackingSystem.Pages.Students.DetailsModel
@{
    ViewData["Title"] = "Student Details";
}

<h1>Student Details</h1>
<div>
    <dl class="row">
        <dt class="col-sm-2">Student #</dt>
        <dd class="col-sm-10">@Model.Student.StudentNumber</dd>
        <dt class="col-sm-2">Full Name</dt>
        <dd class="col-sm-10">@Model.Student.FullName</dd>
        <dt class="col-sm-2">Grade Level</dt>
        <dd class="col-sm-10">@Model.Student.GradeLevel</dd>
        <dt class="col-sm-2">Contact</dt>
        <dd class="col-sm-10">@Model.Student.ContactEmail</dd>
    </dl>
    <a asp-page="Index" class="btn btn-secondary">Back to List</a>
</div>
